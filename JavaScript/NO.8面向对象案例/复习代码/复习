复习
	Number:数字
	Boolean:波尔值 （true false） 
	null:空值 
	undefind:未定义的值
	String:字符串 
	存储到了->栈里
	直接赋值
	----------
	Array:数组 
	Object:对象 
	Function:函数
	存到了->堆里
	存储到堆里 不是直接赋值
	把值放到了一个内存地址中  这个对象指向了这块内存地址

	+ - * / % 
	++ -- ：加加减减在前 先计算后赋值 在后先赋值后计算 
	= += -= *= /= %= 
	&& ：两个条件都满足 才算真
	|| ：只要有一个条件 满足 就是真的
	!：真的变假  假的变真
	== != > < >= <= 

	if:用来 判断条件 是真 还是假 （Blooean<true><false>）
	switch:用来判断有多个值 值是不限制类型

	for 
	while
	do while

	字符串
		+
		""+变量名+""
	数组

	函数

	dom:
		document：内置对象 所有操作元素的方法 都是document中的行为和属性
		window:内置对象
		body:内置对象

		createElement(tagName):根据标签的名字  创建一个元素 需要传一个标签的名字

		父元素.appendChild(子元素) 只有当把子元素 放到HTML中存在一个 父元素中  -> 才会显示在HTML文档中

		textContent：在元素中 添加文本内容




